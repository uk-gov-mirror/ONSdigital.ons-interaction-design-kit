---
import BaseBanner from "../PageBanner/BaseBanner.astro";

interface Props {
  caption: string;
  title: string;
  description: string;
}

const { caption, title, description } = Astro.props;
---

<BaseBanner
  variant="topic"
  caption={ caption }
  title={ title}
  description={ description }
/>